# --- Core CV / 3D geometry (M0–M3) ---
numpy>=1.26
opencv-python>=4.10      # ORB, essential matrix, PnP, triangulation
scipy>=1.13              # least_squares (bundle adjustment, M3)
matplotlib>=3.8          # trajectory plots
open3d>=0.18             # 3D point cloud / map visualization
pyyaml>=6.0              # configs/kitti.yaml
tqdm>=4.66               # progress bars

# --- Test ---
pytest>=8.0              # testing